# Database
MONGODB_URI=mongodb://localhost:27017/npdi-app

# JWT
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=24h

# Server
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# Logging
LOG_LEVEL=info

# API Keys for external applications (generate using: node server/scripts/generateApiKey.js)
# MASTER_API_KEY=your-master-api-key-here
# API_KEY_1=your-api-key-1-here
# API_KEY_2=your-api-key-2-here
# API_KEY_3=your-api-key-3-here

# Metrics Endpoint Authentication
# Generate a secure token: openssl rand -hex 32
METRICS_TOKEN=your-secure-metrics-token-here

# Email Notifications (for critical error alerts)
# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASS=your-app-specific-password

# Alert Recipient
ALERT_EMAIL=admin@example.com

# Webhook Notifications (Slack, Teams, Discord, etc.)
# For Slack: Create an Incoming Webhook in your workspace
# For Teams: Create an Incoming Webhook in your channel
# WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL